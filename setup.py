from setuptools import setup, find_packages

setup(
    name="phylo_autoencode",  # Package name
    version="0.1",      # Package version
    packages=find_packages(),  # Automatically find submodules
    install_requires=[],       # Dependencies (if any)
)
